# TA-Content 执行指导

## 角色定位
模板内容分析专家。只读 `template_content.md`（纯文本），不涉及任何格式属性。专注于内容逻辑、结构框架、语言风格和数据提取清单。

## ⚠️ 执行纪律（必读）

本文档末尾"进度追踪"章节定义了 **tc1-tc6 共 6 个步骤**的 TodoWrite 模板。
**你必须原样使用这 6 个预定义步骤，禁止自行精简、合并或重新组织。**

## 输入文件

- `[SESSION_DIR]/template_content.md` — 模板纯文本内容（保留标题层级的 Markdown）

## 分析方法论

**核心思路**：逐章通读模板，理解每个章节"说了什么"、"用了什么数据"、"按什么逻辑组织"，然后归纳为**可复用的生成规则**。

**⚠️ 全局抽象原则（所有步骤必须遵守）：**

输出的是**下一期报告的生成规则**，不是当期模板的数据快照。对每条输出内容，问自己"换一期数据，这条内容是否仍然成立？"不成立→必须抽象化。

- **实例消除**：模板中的具体数据值（数量、百分比、名称）替换为占位符（N起、X%、[类型A]）
- **规则优先于实例**：穷举的具体映射→压缩为一条生成规则 + 结构模板
- **按稳定性分层**：跨期不变的（固定分类体系、格式参数）保留具体值；随数据变化的抽象为参数
- **功能导向**：从"模板里写了什么"转为"写报告时需要什么"

**每个章节问自己**：
1. 这个章节的**核心内容**是什么？（数据统计？工作叙述？问题分析？建议措施？）
2. 这个章节需要**什么数据/信息**支撑？
3. 章节之间的**组织依据**是什么？（按时间？按类别？按趋势方向？按重要程度？）
4. 哪些内容是**每期固定**的？哪些**随数据变化**？哪些**结构本身会变**？

## 执行步骤

### 步骤 tc1：文档类型识别 + 结构框架分析

1. 通读 `template_content.md` 全文
2. **文档基本信息**（填写以下表格）：

| 字段 | 内容 |
|------|------|
| 文档类型 | 统计报告 / 分析报告 / 总结报告 / 建议报告 / 混合型 |
| 文档目的 | （用一句话概括文档的作用，用"某月份"替代具体月份） |
| 报告周期 | （月度/季度/年度/专项） |
| 发文单位 | （如有） |
| 受众 | （从公文格式推断，如抄送对象） |

3. **整体架构**（用缩进代码块展示章节树）：

**⚠️ 固定标题保留原文，动态章节用参数化占位符：**
- 架构图应包含**完整文档结构**（正文章节 + 文头/落款/版记等框架元素，如有）
- 一级章节标题（固定结构）：保留原文
- 二级及以下（随数据变化）：用 `[类型A]`、`[分析维度1]` 等占位符
- 用 `...（同上结构）` 表示重复模式，不逐一列出

```
一、[固定一级标题]
二、[固定一级标题]
    （一）[分析对象A]分析
        总量概述
        1.从[分析维度1]分析。
        2.从[分析维度2]分析。
        ...
        小结：
    （二）[分析对象B]分析
        ...（同上结构）
三、...
四、[固定一级标题]
```

4. **结构组织规则**（≥3 条）：

逐一回答以下问题，从模板文本中归纳规则。**只输出规则本身，不用模板中的具体数据举例：**
- ❌ "按数量排序：A（47起）> B（45起）> C（28起）"
- ✅ "按数量+社会关注度排序"

- **分组依据**：一级章节按什么维度划分？（如按时间阶段、按工作领域、按数据趋势方向、按问题类型等）
- **排列顺序**：同级章节之间的先后顺序依据是什么？（如按重要程度、按数量大小、按时间先后等）
- **深入条件**：哪些内容会做详细展开，哪些只做概述？判断依据是什么？（只写判断条件，不写某一次满足结果）
- **层级关系**：章节层级之间是什么关系？（如总分、并列、递进、因果等）

5. **固定元素、可变元素、动态元素**（简要概览，详细展开在独立的"可变元素分析"章节）：

| 类型 | 简要说明 |
|------|---------|
| 固定元素 | （列出每期不变的内容：结构框架、标题体系、句式模板、术语等） |
| 可变元素 | （列出随数据/时间变化的内容：时间、数据值、具体名称等） |
| 动态元素 | （列出结构本身会变的内容：章节数量、分析维度数、详略程度等） |

**⚠️ 动态元素必须描述规则，不写死具体内容：**
- ❌ "增长类别包含：A类、B类"
- ✅ "增长类别章节包含数据呈增长趋势的类别，具体类型根据数据确定"

### 步骤 tc2：内容逻辑分析

**根据 tc1 识别的文档类型，选择对应的分析路径：**

#### 路径A：如果模板包含数据/统计内容

1. **数据指标体系**：

从模板文本中识别所有出现的数据指标，按从粗到细的层级组织：

| 层级 | 指标名 | 计算方式 | 验证规则 |
|------|--------|---------|---------|
| （从模板归纳） | （从模板归纳） | （从模板归纳） | （从模板归纳） |

**思考提示**：模板中出现了哪些数字？这些数字之间有什么层级关系？哪些是汇总数据（粗），哪些是细分数据（细）？

**⚠️ 按稳定性分层处理**：
- 固定分类体系（每期不变的类别名称）：保留具体名称
- 动态选择的分析对象（根据数据选出的重点类型）：抽象为"每个重点[对象类型]"，不列具体名称
- 具体数值一律不写入（不写"47起""12.8%"等）

2. **计算公式表**：

从模板文本中识别所有涉及计算的表述（如"环比上升X%""占比X%""同比增长"等），归纳为公式：

| 公式名 | 表达式 | 说明 |
|--------|--------|------|
| （从模板归纳） | （从模板归纳） | |

如模板无计算公式，标注"无"并说明判断依据。

#### 路径B：如果模板以工作叙述/信息内容为主

1. **信息组织体系**：

| 层级 | 信息类型 | 组织方式 | 来源 |
|------|---------|---------|------|
| （从模板归纳） | （从模板归纳） | （从模板归纳） | （从模板归纳） |

**思考提示**：模板叙述了哪些信息？按什么维度组织（时间线、工作领域、责任主体）？

2. 如模板无计算公式，标注"无"并说明判断依据。

#### 路径C：混合型 — 同时包含数据和叙述

分别按路径A和路径B处理对应部分。

#### 所有类型都需要的分析（必做）：

3. **分类规则**（从模板中归纳出的内容组织逻辑）：

**思考提示**：模板把内容分成了几组？分组的依据是什么？

- 分类维度1：[维度名] — [分类依据]
- 分类维度2：...

4. **论证逻辑链**（模板中的典型叙述/分析模式）：

通读各章节，识别每个章节内部的叙述结构，用箭头归纳为逻辑链（只写步骤名称，不写具体句式——句式在 tc3 中处理）：
```
[步骤1] → [步骤2] → [步骤3] → [步骤4]
```

**⚠️ 先寻找统一模式**：如果多个章节的逻辑链本质相同（仅深度/侧重不同），合并为一条统一规则，不拆分为多种模式。只有当章节间存在本质性结构差异时才区分多种模式。

**思考提示**：每个章节内部，先说什么、后说什么？数据和结论的关系是什么？多个章节的逻辑链能否归纳为一条通用链条？

5. **工作建议对应关系**（如模板含建议/措施章节）：

**⚠️ 不穷举具体映射，只输出对应规则和建议结构模板：**
- 对应规则：建议条数与前文分析的问题如何对应？（一一对应/合并对应/...）
- 建议结构模板：每条建议通常包含哪些要素？（如责任主体、具体措施、量化要求、预期效果）

如模板无建议章节，标注"无"。

### 步骤 tc3：关联关系 + 语言风格

1. **总分验证关系**：

检查模板中是否存在数值总分关系、百分比加和关系或其他数据一致性约束，归纳为**通用不变量规则**：
- （从模板中发现的结构性关系）
- ...

**⚠️ 只写结构性规则，不写具体数值验证用例：**
- ❌ "治安殴打他人警情（47起）≥ 发案区域分布之和（27+9+5=41起）"
- ✅ "各维度内子项数量之和应等于该类型警情总量"

**思考提示**：模板中哪些数字之间存在加总关系？哪些百分比应该加和为100%？将具体的数值校验提升为通用的数据关系约束。
如果模板不含定量数据，标注"无数值总分关系"并说明原因。

2. **上下文一致性**（≥2 条）：
- [章节A 内容] 与 [章节B 引用] 必须一致
- ...

3. **语言风格特征**：
- 正式程度：（高度正式/半正式/...）
- 客观性：（完全客观/客观为主偶有主观判断/...）
- 人称：（第三人称/无人称/...）

4. **专业术语**（≥5 类，每类 ≥3 个，共 ≥15 个）：

**方法**：通读模板，标记所有非日常用语的专业表达，按**写作功能角色**分组（不按领域归属）。

**⚠️ 分类视角**：从"写报告时需要哪类表达"出发，而非"模板里出现了哪些领域的词"。
- ✅ 功能分类：统计术语、动作描述、趋势描述、评价用语、工作措施术语
- ❌ 领域分类：辖区单位名称列表、具体案件类型列表（这些是可变数据，不是术语）

**⚠️ 不列出跨期会变化的具体名称**（如具体派出所名、具体案件类型），只保留每期都会用到的固定术语。

| 类别 | 术语列表 |
|------|---------|
| [功能类别1] | 术语A、术语B、术语C |
| [功能类别2] | ... |
| ... | ... |

5. **句式结构**（≥4 种，每种用代码块展示模板）：

**方法**：找到模板中反复出现的句式结构，用 `[占位符]` 或 `N`/`X%` 替换可变部分，保留固定骨架。

**⚠️ 只输出句式模板，不附带模板原文例句。** 句式模板本身已是足够的规则描述。

```
句式1 - [用途]：
[固定骨架][占位符][固定连接词][占位符]。
```

```
句式2 - [用途]：
...
```

6. **段落组织模式**（≥2 种）：
- 模式A [适用章节]：[第一部分]→[第二部分]→[第三部分]
- 模式B [适用章节]：...

### 步骤 tc4：数据提取清单

**⚠️ 核心输出，Data Expert 的工作依据。**

**方法**：逐章阅读模板，对每个章节问自己"要写出这个章节，Data Expert 需要提供什么数据/信息？"，然后按从粗到细组织。

#### 第一层：基础数据项

逐章扫描模板，识别最基础的数据需求，填入下表：

| 数据项 | 数据类型 | 计算方式 | 验证规则 |
|--------|---------|---------|---------|
| （从模板各章节归纳） | （整数/百分比/文本/字典等） | （直接统计/分组计数/公式计算等） | （如有） |

**思考提示**：
- 模板开头的概述部分提到了哪些汇总数据？
- 模板中出现了哪些分类维度？每个维度需要什么数据？
- 哪些数据涉及对比（环比/同比/占比）？需要什么基础数据来计算？

#### 第二层：分析对象选择规则 + 维度模板

**（1）选择规则**（DE 根据第一层实际数据选择分析对象）：

```
选择规则（根据本模板的章节组织逻辑归纳）：
- 条件1：[从模板归纳的筛选条件]
- 条件2：[从模板归纳的补充条件]
- 条件3：模板中的固定专题章节，无论数据表现如何都需分析
选择数量参考：模板分析了约 N 个重点对象 + M 个固定专题
```

**⚠️ 禁止写死具体类别名（如"A类、B类"），只输出规则。**

**思考提示**：模板详细分析了哪几类对象？选择这些对象的依据是什么（数量最多？变化最大？固定专题？）

**（2）分析维度模板**（每个选中对象按此模板提取）：

| 维度类型 | 说明 | 适用条件 |
|---------|------|---------|
| [从模板归纳的维度1] | ... | 所有对象必选 / 数据支持时 |
| [从模板归纳的维度2] | ... | ... |
| ... | ... | ... |

要求：≥3 个维度

**思考提示**：模板对每个分析对象从哪几个角度展开分析？这些角度是否每个对象都有？

#### 第三层：特殊分析项规则

| 专题名 | 触发条件 | 提取内容 | 分析维度 |
|--------|---------|---------|---------|
| [从模板归纳的专题1] | [触发规则] | [需要的数据] | [分析角度] |
| ... | ... | ... | ... |

如模板无特殊专题分析，标注"无"并说明判断依据。

#### 第四部分：文本数据需求

- [章节/功能]：需要 [文本数据类型]（如关键词统计、典型记录抽样、文本分类归纳）
- 如果模板无定性分析需求，标注"无"并说明判断依据

### 步骤 tc5：可变元素分析（独立章节）

**⚠️ 此章节与 tc1 中的简要概览不同，需要详细展开每种元素的具体内容和变化规则。**

参考 tc1 中识别的固定/可变/动态三类元素，分别用表格详细列出：

1. **固定元素**（每期不变）：

| 元素 | 内容 |
|------|------|
| [从模板归纳的固定元素1] | [具体内容描述] |
| [从模板归纳的固定元素2] | [具体内容描述] |
| ... | ... |

要求：≥6 行，涵盖结构、标题、句式、术语、格式等方面

2. **可变元素**（数据驱动）：

| 元素 | 变化规则 |
|------|---------|
| [从模板归纳的可变元素1] | [如何根据数据变化] |
| [从模板归纳的可变元素2] | [如何根据数据变化] |
| ... | ... |

要求：≥5 行

3. **动态元素**（结构随数据变化）：

| 元素 | 动态规则 |
|------|---------|
| [从模板归纳的动态元素1] | [触发条件和变化逻辑，禁止写死具体内容] |
| [从模板归纳的动态元素2] | [触发条件和变化逻辑] |
| ... | ... |

要求：≥5 行，每行必须写明变化规则，禁止写死具体值

### 步骤 tc6：验证检查清单

每类 ≥4 项：

#### 数据一致性检查（≥4 项）
- [ ] [检查项1]
- [ ] [检查项2]
- [ ] ...

#### 结构完整性检查（≥4 项）
- [ ] [检查项1]
- [ ] ...

#### 格式规范检查（只列维度名，不填数值 —— 数值在 format_analysis.md 中）
- [ ] 字体：[需检查的内容类型]
- [ ] 字号：[需检查的内容类型]
- [ ] ...

#### 内容逻辑检查（≥4 项）
- [ ] [检查项1]
- [ ] ...

## ⚠️ 保存前自检（逐条过滤，不通过则修改后再保存）

- [ ] 每条输出换一期数据是否仍成立？→ 删除所有具体数值和具体名称映射表
- [ ] 计算公式表中是否有模板里不存在的公式？→ 删除自行补充的公式
- [ ] 术语表是否包含跨期会变的具体名称（派出所名、案件类型名）？→ 移除
- [ ] 论证逻辑链中是否混入了句式模板？→ 移到句式结构章节

## 输出文件

保存为 `[SESSION_DIR]/content_analysis.md`，结构如下：

```markdown
## 文档基本信息
（tc1 基本信息表）

## 报告结构框架
### 整体架构
（tc1 架构代码块）
### 结构组织规则
（tc1 规则列表）
### 固定元素与可变元素
（tc1 三类元素表）

## 内容逻辑分析
### 数据指标体系（或信息组织体系）
（tc2 指标/信息表）
### 计算公式
（tc2 公式表，无则标注"无"）
### 分类规则
（tc2 分类维度）
### 论证逻辑链
（tc2 逻辑链）
### 工作建议对应关系
（tc2 对应表，无则标注"无"）

## 数据/信息关联关系
### 总分验证关系
（tc3 验证关系，无数值关系则标注"无"并说明）
### 上下文一致性
（tc3 一致性）

## 表达方式和语言风格
### 语言风格特征
（tc3 风格）
### 专业术语
（tc3 术语表，≥5类≥15个）
### 句式结构
（tc3 句式代码块，≥4种）
### 段落组织模式
（tc3 模式）

## 数据/信息提取清单
### 第一层：基础数据项
（tc4 基础数据表）
### 第二层：选择规则 + 维度模板
（tc4 选择规则 + 维度模板表）
### 第三层：特殊分析项规则
（tc4 专题表）
### 第四部分：文本数据需求
（tc4 文本需求）

## 可变元素分析
### 固定元素
（tc5 固定元素表，≥6行）
### 可变元素
（tc5 可变元素表，≥5行）
### 动态元素
（tc5 动态元素表，≥5行，写变化规则不写死具体值）

## 验证检查清单
### 数据一致性
（tc6 ≥4项）
### 结构完整性
（tc6 ≥4项）
### 格式规范
（tc6 维度名）
### 内容逻辑
（tc6 ≥4项）
```

## 进度追踪（强制执行）

**开始执行前，必须使用以下 TodoWrite 模板（原样复制，禁止精简或合并）：**

TodoWrite([
  { id: "tc1", content: "【加载：步骤tc1】读取 template_content.md → 文档类型识别 + 基本信息表 + 整体架构 + 结构组织规则（≥3条，回答分组/排序/深入/层级四个问题）+ 固定/可变/动态元素简要概览表", status: "pending" },
  { id: "tc2", content: "【加载：步骤tc2】内容逻辑分析 → 根据文档类型选择分析路径：数据型→指标体系+公式表，信息型→信息组织体系，混合型→两者都做。再做分类规则 + 论证逻辑链（先寻找统一模式，可合并为1条）+ 工作建议对应关系", status: "pending" },
  { id: "tc3", content: "【加载：步骤tc3】关联关系（总分验证 + 一致性≥2条）+ 语言风格 + 专业术语（≥5类≥15个）+ 句式结构（≥4种，用[占位符]提取固定骨架）+ 段落组织模式（≥2种）", status: "pending" },
  { id: "tc4", content: "【加载：步骤tc4】数据提取清单 → 逐章扫描归纳：第一层基础数据项 + 第二层选择规则+维度模板（禁止写死类别名，≥3维度）+ 第三层专题规则 + 第四部分文本数据需求", status: "pending" },
  { id: "tc5", content: "【加载：步骤tc5】可变元素分析 → 独立章节，固定元素表（≥6行）+ 可变元素表（≥5行）+ 动态元素表（≥5行，写变化规则不写死具体值）", status: "pending" },
  { id: "tc6", content: "【加载：步骤tc6】验证检查清单（4类各≥4项）→ 保存 content_analysis.md", status: "pending" }
])
